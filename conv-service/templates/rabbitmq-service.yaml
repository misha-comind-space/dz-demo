apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.database.host }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.database.port }}
      protocol: TCP
  selector:
    components: {{ .Values.database.components }}
